# AGENTS.md

## 项目定位

这是一个“土族盘绣纹样开放素材库”项目。

目标是用全新代码，开发一个前台体验高度参考数字敦煌开放素材库的网站，并配套自己的后台 CMS、后端接口、MySQL 数据库、文件上传与直接下载能力。

参考的是页面结构、检索方式、内容组织和视觉气质，不是复制原网站源码、接口、后台或数据库。

## 固定技术栈

- 前台：Vue 3 + Vite + TypeScript
- 后台：Vue 3 + Vite + TypeScript + Element Plus
- 后端：Node.js + NestJS
- 数据库：MySQL
- 文件存储：本地 uploads 目录
- 目标：本地可运行

## 目录约定

- frontend/：前台展示网站
- admin/：后台管理系统
- backend/：后端接口服务
- database/：SQL 与种子数据
- uploads/：上传文件
- docs/：中文项目文档

## 核心产品规则

- 前台栏目包括：首页、纹样素材、文献资料、共创作品、传承人介绍、应用案例、关于平台。图库、盘绣专题、图案元素、工艺技法不属于当前版本。
- 前台正式内容必须来自真实接口，不允许用静态假数据冒充正式功能。
- 后台上传、编辑、上架内容后，前台必须自动显示。
- 后台下架、删除内容后，前台必须不显示。
- 检索是核心能力：列表页至少支持关键词、分类、标签、排序、分页；纹样等模块还要支持色彩、工艺、应用部位、寓意等筛选。
- 首页、检索页、详情页、专题页的布局和气质要尽量接近参考站点，具体参考 docs/00-原网站参考说明.md。

## 下载规则

- 不需要普通用户登录。
- 不需要下载申请。
- 不需要弹窗确认。
- 不需要订单或支付。
- 某条内容存在 download_file 时，前台显示下载按钮。
- 没有 download_file 时，不显示下载按钮。
- 点击后直接下载。
- 可以统计 download_count，但不是核心目标。

## 明确禁止

不要开发：

- 普通用户注册、普通用户登录、用户中心
- 我的素材、购物车、订单、支付、发票
- 会员、评论、在线授权申请、下载审核、区块链证书
- 任何依赖原网站接口、数据库、线上打包代码的实现

## 工程规则

- 不要重建项目。
- 不要重复初始化已有目录。
- 不要随意更换技术栈。
- 不要同时让多个对话修改同一目录。
- 数据库字段、后端 DTO、后台表单、前台展示字段必须一致。
- 新增或修改功能时，同步更新对应 docs 文档。
- 优先小步提交：先让功能可运行，再补细节。
- 对复杂任务先拆分阶段，不要一次完成全部。

## 完成标准

每次任务完成后都必须说明：

1. 修改了哪些文件
2. 如何运行或验证
3. 已完成功能
4. 未完成项与剩余风险

项目最终完成时必须满足：

- backend、admin、frontend 能本地启动
- MySQL 能初始化，seed.sql 可导入
- 默认管理员可登录后台
- 后台可上传图片、PDF、Word、ZIP
- 前台能读取真实接口并展示内容
- 下架内容前台不显示
- 有 download_file 的内容可以直接下载
